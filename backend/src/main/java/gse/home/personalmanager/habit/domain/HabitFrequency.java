package gse.home.personalmanager.habit.domain;

public enum HabitFrequency {
    DAILY,    // Can be logged every day
    WEEKLY,   // Can be logged on specific days of the week
    CUSTOM    // For future flexibility (monthly, etc.)
}
